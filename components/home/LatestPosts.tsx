import Link from "next/link";
import Image from "next/image";
import { ArrowUpRight } from "lucide-react";
import { getPosts } from "@/lib/posts";
import { SectionTitle } from "@/components/ui/SectionTitle";
export function LatestPosts(){const posts=getPosts();const featured=posts[0];if(!featured)return null;return <section id="journal" className="home-section"><SectionTitle number="01" title="最近写下的" english="THE JOURNAL" href="/blog/" link="全部文章"/><div className="journal-grid"><Link href={`/blog/${featured.slug}/`} className="featured-post"><Image src={featured.cover} alt={featured.title} fill sizes="(max-width: 700px) 100vw, 60vw"/><div className="featured-shade"/><span className="featured-tag">精选手记 <span>FEATURED</span></span><div className="featured-post-text"><div className="post-meta">{featured.tags.join(" & ").toUpperCase()}<span>{featured.date.replaceAll("-",".")} · {featured.readingTime} MIN READ</span></div><h3>{featured.title}</h3><p>{featured.description}</p></div><ArrowUpRight className="feature-arrow" size={22}/></Link><div className="post-side">{posts.slice(1,3).map((p,i)=><Link href={`/blog/${p.slug}/`} className="small-post" key={p.slug}><div className="post-meta"><span>{p.tags.map(tag=>tag.toUpperCase()).join(" / ")}</span><span>0{i+2}</span></div><h3>{p.title}<ArrowUpRight size={17}/></h3><p>{p.description}</p><span className="post-date">{p.date.replaceAll("-",".")} <i/> {p.readingTime} min read</span></Link>)}</div></div></section>}
